version: '3.5'

services:
  react-app:
    container_name: react-app
    build:
      context: .
      dockerfile: Dockerfile-local
    volumes:
      - './src:/usr/src/app'
      - '/usr/src/app/node_modules'
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=development